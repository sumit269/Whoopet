<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/section_margin">

    <com.laser.views.EditTextView
        android:id="@+id/email_address"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:labelDrawable="@drawable/common_ic_googleplayservices"
        app:labelText="Email address" />

    <com.laser.views.EditTextView
        android:id="@+id/create_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/section_margin"
        app:labelDrawable="@drawable/common_ic_googleplayservices"
        app:labelText="Password" />

    <com.laser.views.EditTextView
        android:id="@+id/confirm_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/section_margin"
        app:labelDrawable="@drawable/common_ic_googleplayservices"
        app:labelText="Confirm password" />

    <com.laser.views.EditTextView
        android:id="@+id/first_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/section_margin"
        app:labelDrawable="@drawable/common_ic_googleplayservices"
        app:labelText="First Name" />

    <Button
        android:id="@+id/btn_register"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/register"/>

</LinearLayout>